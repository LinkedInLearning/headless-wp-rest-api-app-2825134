import config from './modules/config.js';

/**
 * Single task view script
 * 
 * If the user is authenticated, check to see if there is a task id.
 * - If there is a task ID, get the task.
 * - If not, allow the user to create a new task.
 * 
 * @package Taskbook
 */
